package trafficsim.factories;


public class GreenMile {
	/*
			//GREEN MILE
			//Green mile will have a length of lengthofgm stop lights where lengthofgm is set to 5 as default
			//starting point will be a spawn point and the direction will allow cars that go straight forward to benefit from the green mile
			
			int lengthOfGm;
			Vertex<Road> startingPoint;
			Vertex<Road> nextPoint;
			
			public void GreenMileMaker(TrafficSimWorld world, int lengthOfGm){
				int length = lengthOfGm;
				
				Vertex<Road> startingPoint = world.getSpawnPoints().get(0);
				startingPoint.getAdjacentEdges();
				for(Vertex<Road> nextPoint : startingPoint.getAdjacentVertexIterator())
				{
				
				}
				
				for(int i = 0; i < lengthOfGm; i++){
					GreenMileLightChanger();
				}
				
			}
			public void GreenMileLightChanger(){
				startingPoint.getData().
				
				nextPoint = startingPoint;
				nextPoint = startingPoint.getNeighbor(edge);
			}
			*/

}
